# Project 4 — Schema Evolution

Start with:

```text
event_id
vehicle_id
speed_kph
battery_soc
```

Add:

```text
battery_temperature_c
```

Then create a breaking example.

Document:

```text
change
compatibility
consumer impact
migration
version
deprecation
```

The goal is to understand schema evolution as a lifecycle process.
