# Git Commit — Day 19

```bash
git add Day-19-Docker/
git commit -m "Add Day 19 Docker complete learning module"
git push
```

Optional branch:

```bash
git checkout -b day-19-docker
git add Day-19-Docker/
git commit -m "Add Day 19 Docker complete learning module"
git push -u origin day-19-docker
```
