services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: automotive
      POSTGRES_USER: data_engineer
      POSTGRES_PASSWORD: dev_password

  adminer:
    image: adminer:4
    profiles:
      - tools
    ports:
      - "8081:8080"
