# Dockerfile Patterns

Contains reusable patterns for:

- Python
- non-root execution
- dependency layers
- multi-stage builds
- runtime-only images

Treat these as templates and adapt them to the actual application.
