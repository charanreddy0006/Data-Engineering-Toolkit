# Git Commit — Day 20 Kubernetes

Run tomorrow:

```bash
git add .
git commit -m "Add Day 20 Kubernetes complete learning module"
git push
```

Optional branch:

```bash
git checkout -b Day-20-Kubernetes
```

Verify:

```bash
git status
git log -1 --oneline
```
