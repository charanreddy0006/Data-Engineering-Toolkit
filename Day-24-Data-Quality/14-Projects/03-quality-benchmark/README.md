# Project 3 — Quality Benchmark

Compare pandas, PyArrow, and Spark on progressively larger telemetry datasets.

Measure:

- runtime
- memory
- rows/second
- check coverage

Start at 100 MB, then 500 MB and 1 GB if hardware permits.

Record machine configuration and dataset characteristics so the comparison is meaningful.
