# Project 1 — CSV Quality Gate

Build a pipeline that reads telemetry CSV, profiles it, validates required fields/ranges/duplicates, writes valid records, writes quarantine records, and emits a JSON quality report.

Suggested tools:

- pandas
- Python
- pytest
- JSON

Extension: convert valid output to Parquet and compare row counts, schema, file size, and quality metrics.
