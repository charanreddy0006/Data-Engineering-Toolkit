# Identity and Access Management

## Purpose
IAM manages identities, groups, roles, policies, credentials, federation, workload identities, and reviews.

## Data engineering

Separate human access from machine access. Prefer workload identities or short-lived credentials where supported.

## Lifecycle

Create → Approve → Provision → Review → Modify → Revoke.
