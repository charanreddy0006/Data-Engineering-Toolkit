# Least Privilege

## Purpose
Give identities only the access required for their task.

## Example

A pipeline writing one table should not receive administrator permissions for an entire cloud account.

## Controls

Narrow roles, resource scoping, separate read/write, temporary elevation, and periodic review.
