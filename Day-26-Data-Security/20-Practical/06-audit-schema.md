# Audit Event Schema
A useful audit event can contain principal, action, resource, timestamp, result, source/context, request identifier, and reason where appropriate. Avoid putting sensitive payloads or secrets into the audit event.
