# Security Testing
Useful tests include dependency scanning, secret scanning, access-control tests, configuration checks, TLS validation, container scanning, negative authorization tests, and recovery exercises. Test the controls that matter to the threat model.
