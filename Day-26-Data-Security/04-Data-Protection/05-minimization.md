# Data Minimization

## Purpose
Collect, store, and expose only what the approved purpose needs.

## Example

A fleet dashboard may need region-level aggregates instead of precise customer-linked location.

## Security value

Reducing data volume and linkage can reduce incident impact.
