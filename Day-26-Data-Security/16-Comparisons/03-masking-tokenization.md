# Masking vs Tokenization

## Purpose
Masking changes exposed representation; tokenization replaces a value with a controlled surrogate.

## Use

Masking is common for display/test access; tokenization is useful when stable surrogate references are needed without exposing original values.
